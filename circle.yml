## Customize dependencies
dependencies:
  override:
    - gem install rspec

## Customize database setup
database:
  override:
    - echo "no database step up needed. skipping"

## Customize test commands
test:
  override:
    - rspec
